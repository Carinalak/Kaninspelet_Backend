{
  "compilerOptions": {
    "target": "es2016",                                 /* Sätt JavaScript-versionen för den kompilerade koden. */
    "module": "commonjs",                               /* Specifierar modulformatet som genereras. */
    "outDir": "./dist",                                  /* Sätt utmapp för den genererade JavaScript-koden (dist/). */
    "rootDir": "./src",                                  /* Sätt källmappen för TypeScript-koden (src/). */
    "esModuleInterop": true,                             /* Aktivera import av CommonJS-moduler. */
    "forceConsistentCasingInFileNames": true,            /* Tvinga konsekvent filnamnsstorlek i importerna. */
    "strict": true,                                      /* Aktivera striktare typkontroller. */
    "skipLibCheck": true,                                 /* Hoppa över typkontroll för .d.ts-filer. */
    "resolveJsonModule": true
  },
  "include": ["src/**/*"],                               /* Inkludera alla TypeScript-filer i src/ för kompilering. */
  "exclude": ["node_modules"]                            /* Exkludera node_modules från kompileringen. */
}
